<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fechaPedido %><br>
    <%= f.date_select :fechaPedido %>
  </div>
  <div class="field">
    <%= f.label :fechaEntrega %><br>
    <%= f.date_select :fechaEntrega %>
  </div>
  <div class="field">
    <%= f.label :fechaEntregado %><br>
    <%= f.date_select :fechaEntregado %>
  </div>
  <div class="field">
    <%= f.label :importetotal %><br>
    <%= f.text_field :importetotal %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
